# EUROSFERA — ХЭНДОФФ для нового чата

> Если Анатолий пишет «продолжить делать сайт» — прочитай этот файл и память
> `project_eurosfera_website.md`, и сразу продолжай. НЕ проси объяснять заново.

## ✅ ОБНОВЛЕНО 2026-06-11 (окно d3fff91c) — cache-bust v=0611d
- **goods**: «Что мы закупаем» → «Что мы **продаём**». Ferro и **TECE** — фирменные SVG-плитки (не дублируют фото GROHE).
  Новый блок **#popular** «Террасный керамогранит 2.0» с РЕАЛЬНЫМИ фото (Pexels, своб. лицензия):
  `assets/img/terrace-hero.jpg` + врезка `terrace-tile.jpg`; свойства, форматы 20/30 мм, тренд-фактуры. (research NL/BE/PL).
- **transfer**: убрана мультяшная 3D `car.glb`. Заставка = чистый **новый S-Class без номеров** (`car-sclass.jpg`).
  ⚠️ `car-vclass.jpg` НЕ использовать на витрине — там турецкий флаг + номер «TR».
  **Видео-слой готов**: как только появится `assets/video/vclass-arrival.mp4` — он сам заменит фото и
  ПРОКРУЧИВАЕТСЯ скроллом (`arrival.js` HEAD-проверка + scroll-scrub). Анатолий генерит ролик ИИ-видео
  (Kling/Runway/Sora/Veo) по промпту в `assets/video/КАК_ДОБАВИТЬ_ВИДЕО.md`. Это его выбор (а не фото).
- **agro**: у каждого удобрения межд. аббревиатура (AN/CN/CAN/NPK/perlite/vermiculite) + «Где применяется» (SEO ЕС).
- **Глобально** (enhance.css/js, все 8 стр.): `text-wrap:balance/pretty` (грамотные переносы); **микро-звуки**
  на значимые клики (WebAudio, кнопка mute `.e-snd` слева внизу); **телефон под «дымкой»** — `.phone-mask`
  раскрывается по клику (цифр в HTML НЕТ, base64 в `data-p`). Кнопка «наверх» `.e-top` уже была.
- Новые файлы: `arrival.js`, `assets/video/`. Версии ассетов = `?v=0611d`.

## Что это
Мультинаправленный премиум-сайт компании EUROSFERA (статика HTML/CSS/JS, без бэкенда).

- **Рабочая папка:** `/Users/tukananatoliy/Documents/IT ПРОЕКТ АГЕНТ/eurosfera-site-v2/`
- **Приватный репо:** `GAGAUZ113/eurosfera-site` (remote `origin`)
- **Публичное демо:** `GAGAUZ113/eurosfera-demo` → **https://gagauz113.github.io/eurosfera-demo/** (remote `demo`)
- **ВАЖНО: пушить в ОБА remote после каждого изменения:** `git push origin main && git push demo main`
- Локальный просмотр: `python3 -m http.server 8778` в папке → http://localhost:8778
- Коммитить от: `git -c user.email="claude@euro.s.bg" -c user.name="Anatoly (Claude Code)"`, в конце сообщения `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

## Страницы (8 + 404)
index (хаб), goods (товары), logistics (логистика), it (IT/ПО), company (открытие фирмы в Болгарии), agro (удобрения), transfer (Premium-трансфер), music (муз. дистрибуция), 404.

## Что уже сделано
- 3D: глобус (`globe.js`), 3D-объект на IT (`ribbon.js`), 3D-машина model-viewer (`assets/models/car.glb`), 3D scroll-сцена (`scene3d.js`).
- Lenis (плавный скролл), кастомный курсор, прелоадер, аврора-фон, прогресс-бар, Typed.js, vanilla-tilt — всё в `enhance.js`/`enhance.css`, библиотеки локально в `lib/`.
- 9 языков (RU/EN/BG/UK/PL/DE/RO/ES/TG) — `i18n.js`, полный автоперевод страницы через Google Translate (cookie-метод). ⚠️ Это для ПОСЕТИТЕЛЕЙ, не для SEO (поисковики видят только русскую версию).
- Разные переходы между направлениями (wipe/iris/bars + цвет) — в `enhance.js`.
- IT: каталог ПО с поиском/фильтром/«Запросить» (масштаб 50-60+) + кинематографичный 3D-hero при входе.
- Товары: слайдер фабрик (Cersanit/Opoczno/GROHE/Cerrad/Kospel/Ferro) + bento с фото электроники/стройматериалов.
- Логистика: крупные блоки image/text (стиль dongwon). Трансфер: конфигуратор авто + онлайн-заказ + 3D-машина.
- SEO: Schema.org (Organization) на 8 стр., `sitemap.xml`, `robots.txt`, ключевые слова.
- Контакты: `a.tukan@euro.s.bg`, Telegram `@gagauz13`, +359 89 209 84 60, WhatsApp 359892098460.

## ⚠️ ГЛАВНОЕ, ЧЕГО АНАТОЛИЙ ХОЧЕТ
> 🟢 ОБНОВЛЕНО 2026-06-09: пункты 1–3 в значительной части СДЕЛАНЫ (новая заставка, цвета направлений,
> мобайл-фикс, scroll-эффекты) — подробности в разделе «СДЕЛАНО В НОЧЬ 2026-06-09» ниже. Осталось — оценить глазами и усилить.

Он говорит: «сайт неплохой, но НЕТ ВАУ-эффекта и заставок как на референсах; не видно, что это делали супер-профи». Нужны:
1. **Кинематографичная ЗАСТАВКА (интро-прелоадер)** уровня референсов — был в процессе апгрейд (логотип + счётчик 0→100% + шторка-раскрытие, цвет под направление). Текущий прелоадер он считает простым.
2. **Сложные «фишки»/спецэффекты и тренды** на входе в направления (он хвалил: liquidtechnology — интро с «открытием жёсткого диска» это их видео `hdd.mp4`, КОПИРОВАТЬ НЕЛЬЗЯ — авторское право; делаем СВОИ в том же духе).
3. Раскатать «вау»-вход (как сделан пилот на IT) на ОСТАЛЬНЫЕ направления, каждое в своём стиле.

**Референсы (нравятся):** aircenter.space, voltlites.com, dongwon.com/en (логистика), liquidtechnology.net (IT — заставка с диском).

**Open-source наработки для легального вдохновения (нашёл):**
- github topics: `awwwards`, `gsap-animation`, `animated-website`, `gsap-scrolltrigger`
- «Gentlerain AI clone» (HTML+GSAP+Three+Lenis), «Nothing Phone showcase» (Three+GSAP+Tailwind), preloader-animation репо (MIT), olivierlarose «awwwards landing» туториалы.
- Брать ТОЛЬКО MIT/open-source приёмы и СВОЙ код; чужие видео/ассеты не копировать.

## Прочие незакрытые задачи
- **Многоязычный SEO**: отдельные индексируемые страницы EN/BG/DE/PL (Анатолий выбрал «делать приоритетные»). Сейчас только автоперевод (не индексируется). Нет API-переводчика — переводить руками или попросить ключ.
- **Публикация на хостинг** SuperHosting.BG → на ПОДДОМЕН euro.s.bg (на основном уже строительный WordPress — НЕ лить поверх). Анатолий даст доступ позже. ZIP готов: `eurosfera-site.zip`.
- **GA4-ID** (аналитика) — ждём от Анатолия. Google-аккаунт для аналитики: `n8n.rus@gmail.com`.
- **Заявки в Telegram-бот** через n8n (webhook) — поле `lead_webhook` в `enhance.js`. Пока форма открывает письмо/Web3Forms.
- **Реальные фото** складов/команды/объектов и логотипы брендов — заменить стоковые (Анатолий пришлёт).
- **Музыка**: Яндекс/Сбер/VK — РФ-площадки, конфликт с правилом «без РФ». Сейчас только мировые (Spotify/Apple/YouTube). Решить.
- **ScrewFast (Astro-шаблон)** — Анатолий выбрал его как базу для направлений, но он НЕ собрался (Node 25 + Tailwind4/rolldown конфликт). Папка `screwfast-base/`. Либо чинить версии, либо адаптировать стиль в нашу статику.

## Правила (железные)
- НЕ упоминать РФ/СНГ/Калининград на сайте EUROSFERA (санкции). Только ЕС + Центральная Азия.
- НЕ показывать ЕИК/VAT компании (защита от мошенников) — уже убраны.
- Фото — отражать реальную (европейскую) команду.
- НЕ перегружать (чисто, понятно, но «дорого»). Уровень — awwwards/Apple.
- Делать самому, не засыпать вопросами; сохранять в GitHub (оба remote).

## Технические заметки
- Playwright/браузер-предпросмотр в прошлой сессии ОТКЛЮЧАЛСЯ → проси Анатолия перезапустить Claude Code, чтобы вернуть «глаза». Скриншоты тяжёлых страниц с WebGL таймаутят — помогает `pkill -f mcp-chrome-*` + перезапуск браузера. Кэш демо/локали — добавлять `?v=N` к URL.
- Node 25 + npm есть. Astro 6/Tailwind4 цепочка ломается — при сборке фиксировать версии.

## ✅ СДЕЛАНО В НОЧЬ 2026-06-09 (автономная сессия, приоритет — МОБАЙЛ)
Анатолий: «в основном заходят с телефона; крупные слова в самом верху обрезаются; проработать ВСЕ
направления, использовать все scroll-эффекты, чтобы потом протянуть через SEO-инструменты». Сделано:
- **ФИКС обрезания на телефоне** (главная жалоба): гигантский водяной знак `.premium3d-word`
  скрыт <480px; глобальный `overflow-x:clip`; `break-word/hyphens` на заголовках; мобильная clamp-типографика
  (крупно, но НЕ гигантски). Проверено инструментами на всех 8 страницах: горизонтального скролла нет (overflow=0).
- **Кинематографичная ЗАСТАВКА v2** (главный нерешённый запрос): счётчик 0→100 + штора (clip-path wipe-up) +
  градиентный логотип, ЦВЕТ под направление, лёгкая на мобиле, безопасна для reduced-motion. Инлайн во всех 9 файлах.
- **Каждое направление — СВОЙ ЦВЕТ** (через `data-page` на <html> → перекрашиваются кнопки/ссылки/рамки/
  прогресс/аврора): logistics=индиго, it=cyan, agro=зелёный, transfer=золото, music=фиолет, company=teal, goods/index=синий.
  + `<meta theme-color>` под направление (адресная строка телефона в тон).
- **Signature-акценты**: золотое подчёркивание заголовков (transfer), CSS-эквалайзер (music), дата-сетка (it).
- **Софит за курсором** (десктоп) цвета направления — приём awwwards.
- **MOBILE PERF**: тяжёлый WebGL (globe/ribbon/scene3d) ВЫКЛЮЧЕН на телефоне/слабых устройствах
  (`html.euro-lite`) → лёгкие CSS-фоллбэки; Lenis и vanilla-tilt только на десктопе. scene3d на мобиле = чистый стек шагов.
- **Премиум scroll-reveal** (тоньше, blur-in) + безопасный desktop-параллакс (hero-photo, водяной знак).
  УСТРАНЁН пред-существующий конфликт `gsap.from`/`.reveal` (заголовки могли застревать невидимыми).
- **Cache-busting `?v=0609f`** на всех ассетах (enhance.css/js, i18n, globe/ribbon/scene3d) — чтобы у Анатолия
  и на демо подтянулось свежее, а не кеш.
- SEO уже было полным (lang/description/canonical/5×OG/4×Twitter/Schema.org/по 1 h1) — не трогал.
- Коммиты `b741970`, `c119415` (+ theme-color) запушены в origin И demo.

⚠️ **Ограничение сессии:** скриншоты MCP-браузера не сохранялись на диск → проверял ПРОГРАММНО
(overflow, размеры шрифтов, цвета, наличие canvas, ошибки консоли) — это точно для вёрстки/перфа,
но «красоту» заставки/софита Анатолий должен оценить глазами на демо.

## ✅ ДОБАВЛЕНО 2026-06-09 (день, по фидбеку Анатолия со скриншотами)
- **БАГ «синий экран»** при скролле/возврате: шторка перехода зависала из-за bfcache (Назад) и якорей.
  Фикс: сброс `.e-pagefade` на `pageshow`/`pagehide` + защита от перехода на ту же страницу. (enhance.js)
- **Повторяющиеся фото товаров** (Cersanit/Opoczno/Cerrad тянули одно фото склада): карточки фабрик →
  единый брендовый стиль (вордмарк на ФИРМЕННОМ цвете бренда, официальные HEX, `translate="no"`),
  без рипа логотипов и фото. CSS `.fab-photo` теперь по `--fab`. (goods.html, enhance.css)
- **Мобильные шрифты ещё мельче** (hero 43→30px, заголовки −20%, поджаты отступы); фикс специфичности `.hero .wrap h1`.
- **Hero-вход** теперь синхронизирован со шторой прелоадера (класс `e-ready`) — кинематографичный въезд.
- **Аналитика-на-почту**: новый модуль `analytics/` (GDPR-логгер с анонимизацией IP + трекинг-пиксель `px.php`
  для статики + крон `daily_report.php` шлёт ежедневное письмо: страницы/IP/источник + README под SuperHosting/cPanel).
  ⚠️ PHP работает ТОЛЬКО на SuperHosting, НЕ на GitHub Pages-демо. Нужно: залить, прописать почту, добавить пиксель на страницы, настроить крон.
- **SEO**: убран дубль `<meta keywords>` (it.html); публичная плашка «отзывы — примеры» заменена на нейтральную.
- ⚠️ Авторские права: интро референсов (aircenter/liquidtechnology) копировать НЕЛЬЗЯ — делаем СВОИ заставки в том же духе (так и объяснено Анатолию).
- Осталось по фидбеку: усилить per-direction анимации входа; дотрим длинных description (it/goods/agro и др. — Google просто обрезает показ); sync og:title↔title на it.html.

## СЛЕДУЮЩИЙ ШАГ (когда вернётся предпросмотр / скажет «продолжить»)
1. Глазами оценить заставку и софит на демо; при желании — усилить «вау» (свои спецэффекты входа per-direction).
2. Многоязычный SEO (отдельные /en/ /bg/ страницы) — всё ещё не сделан (нужен переводчик/ключ).
3. Реальные фото складов/команды/брендов; публикация на поддомен euro.s.bg; GA4-ID; решение по РФ-площадкам музыки.

## 🔄 ПЕРЕДАЧА В ДРУГОЕ ОКНО (2026-06-10, окно db6e2f09)
Если работаешь в другом окне (Анатолий мог переименовать локальную папку, напр. «Eurofer») — чтобы подхватить ВСЁ из этого окна:
1. Это тот же git-репозиторий: `GAGAUZ113/eurosfera-site` (remote origin) + зеркало демо `GAGAUZ113/eurosfera-demo` (remote demo).
2. В своей папке выполни: `git pull origin main` (подтянет последнее). Если папка пустая/новая — `gh repo clone GAGAUZ113/eurosfera-site`.
3. Перед своими правками ВСЕГДА `git pull`, после — `git push origin main && git push demo main` (в ОБА).
4. Последнее сделано: страница «Товары» (goods.html) — премиальные интерьерные фото фабрик (Cersanit/Opoczno/Cerrad/Tubądzin/Aparici/Cerrol — разные) + новая анимационная секция `#buy` «Что мы закупаем» (5 категорий на CSS/SVG: стройматериалы/электроника/агро/IT/нефтепродукты), сделано моделью Fable 5. Стили `.buy-*` в конце `enhance.css`.
5. Демо: https://gagauz113.github.io/eurosfera-demo/ . Локально: `python3 -m http.server 8778`.
