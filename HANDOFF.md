# EUROSFERA — ХЭНДОФФ для нового чата

> Если Анатолий пишет «продолжить делать сайт» — прочитай этот файл и память
> `project_eurosfera_website.md`, и сразу продолжай. НЕ проси объяснять заново.

## Что это
Мультинаправленный премиум-сайт компании EUROSFERA (статика HTML/CSS/JS, без бэкенда).

- **Рабочая папка:** `/Users/tukananatoliy/Documents/IT ПРОЕКТ АГЕНТ/eurosfera-site-v2/`
- **Приватный репо:** `GAGAUZ113/eurosfera-site` (remote `origin`)
- **Публичное демо:** `GAGAUZ113/eurosfera-demo` → **https://gagauz113.github.io/eurosfera-demo/** (remote `demo`)
- **ВАЖНО: пушить в ОБА remote после каждого изменения:** `git push origin main && git push demo main`
- Локальный просмотр: `python3 -m http.server 8778` в папке → http://localhost:8778
- Коммитить от: `git -c user.email="claude@euro.s.bg" -c user.name="Anatoly (Claude Code)"`, в конце сообщения `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

## Страницы (8 + 404)
index (хаб), goods (товары), logistics (логистика), it (IT/ПО), company (открытие фирмы в Болгарии), agro (удобрения), transfer (Premium-трансфер), music (муз. дистрибуция), 404.

## Что уже сделано
- 3D: глобус (`globe.js`), 3D-объект на IT (`ribbon.js`), 3D-машина model-viewer (`assets/models/car.glb`), 3D scroll-сцена (`scene3d.js`).
- Lenis (плавный скролл), кастомный курсор, прелоадер, аврора-фон, прогресс-бар, Typed.js, vanilla-tilt — всё в `enhance.js`/`enhance.css`, библиотеки локально в `lib/`.
- 9 языков (RU/EN/BG/UK/PL/DE/RO/ES/TG) — `i18n.js`, полный автоперевод страницы через Google Translate (cookie-метод). ⚠️ Это для ПОСЕТИТЕЛЕЙ, не для SEO (поисковики видят только русскую версию).
- Разные переходы между направлениями (wipe/iris/bars + цвет) — в `enhance.js`.
- IT: каталог ПО с поиском/фильтром/«Запросить» (масштаб 50-60+) + кинематографичный 3D-hero при входе.
- Товары: слайдер фабрик (Cersanit/Opoczno/GROHE/Cerrad/Kospel/Ferro) + bento с фото электроники/стройматериалов.
- Логистика: крупные блоки image/text (стиль dongwon). Трансфер: конфигуратор авто + онлайн-заказ + 3D-машина.
- SEO: Schema.org (Organization) на 8 стр., `sitemap.xml`, `robots.txt`, ключевые слова.
- Контакты: `a.tukan@euro.s.bg`, Telegram `@gagauz13`, +359 89 209 84 60, WhatsApp 359892098460.

## ⚠️ ГЛАВНОЕ, ЧЕГО АНАТОЛИЙ ХОЧЕТ И ЧЕГО ПОКА НЕТ
Он говорит: «сайт неплохой, но НЕТ ВАУ-эффекта и заставок как на референсах; не видно, что это делали супер-профи». **Это главный нерешённый запрос.** Нужны:
1. **Кинематографичная ЗАСТАВКА (интро-прелоадер)** уровня референсов — был в процессе апгрейд (логотип + счётчик 0→100% + шторка-раскрытие, цвет под направление). Текущий прелоадер он считает простым.
2. **Сложные «фишки»/спецэффекты и тренды** на входе в направления (он хвалил: liquidtechnology — интро с «открытием жёсткого диска» это их видео `hdd.mp4`, КОПИРОВАТЬ НЕЛЬЗЯ — авторское право; делаем СВОИ в том же духе).
3. Раскатать «вау»-вход (как сделан пилот на IT) на ОСТАЛЬНЫЕ направления, каждое в своём стиле.

**Референсы (нравятся):** aircenter.space, voltlites.com, dongwon.com/en (логистика), liquidtechnology.net (IT — заставка с диском).

**Open-source наработки для легального вдохновения (нашёл):**
- github topics: `awwwards`, `gsap-animation`, `animated-website`, `gsap-scrolltrigger`
- «Gentlerain AI clone» (HTML+GSAP+Three+Lenis), «Nothing Phone showcase» (Three+GSAP+Tailwind), preloader-animation репо (MIT), olivierlarose «awwwards landing» туториалы.
- Брать ТОЛЬКО MIT/open-source приёмы и СВОЙ код; чужие видео/ассеты не копировать.

## Прочие незакрытые задачи
- **Многоязычный SEO**: отдельные индексируемые страницы EN/BG/DE/PL (Анатолий выбрал «делать приоритетные»). Сейчас только автоперевод (не индексируется). Нет API-переводчика — переводить руками или попросить ключ.
- **Публикация на хостинг** SuperHosting.BG → на ПОДДОМЕН euro.s.bg (на основном уже строительный WordPress — НЕ лить поверх). Анатолий даст доступ позже. ZIP готов: `eurosfera-site.zip`.
- **GA4-ID** (аналитика) — ждём от Анатолия. Google-аккаунт для аналитики: `n8n.rus@gmail.com`.
- **Заявки в Telegram-бот** через n8n (webhook) — поле `lead_webhook` в `enhance.js`. Пока форма открывает письмо/Web3Forms.
- **Реальные фото** складов/команды/объектов и логотипы брендов — заменить стоковые (Анатолий пришлёт).
- **Музыка**: Яндекс/Сбер/VK — РФ-площадки, конфликт с правилом «без РФ». Сейчас только мировые (Spotify/Apple/YouTube). Решить.
- **ScrewFast (Astro-шаблон)** — Анатолий выбрал его как базу для направлений, но он НЕ собрался (Node 25 + Tailwind4/rolldown конфликт). Папка `screwfast-base/`. Либо чинить версии, либо адаптировать стиль в нашу статику.

## Правила (железные)
- НЕ упоминать РФ/СНГ/Калининград на сайте EUROSFERA (санкции). Только ЕС + Центральная Азия.
- НЕ показывать ЕИК/VAT компании (защита от мошенников) — уже убраны.
- Фото — отражать реальную (европейскую) команду.
- НЕ перегружать (чисто, понятно, но «дорого»). Уровень — awwwards/Apple.
- Делать самому, не засыпать вопросами; сохранять в GitHub (оба remote).

## Технические заметки
- Playwright/браузер-предпросмотр в прошлой сессии ОТКЛЮЧАЛСЯ → проси Анатолия перезапустить Claude Code, чтобы вернуть «глаза». Скриншоты тяжёлых страниц с WebGL таймаутят — помогает `pkill -f mcp-chrome-*` + перезапуск браузера. Кэш демо/локали — добавлять `?v=N` к URL.
- Node 25 + npm есть. Astro 6/Tailwind4 цепочка ломается — при сборке фиксировать версии.

## СЛЕДУЮЩИЙ ШАГ (когда скажет «продолжить»)
Делать **ВАУ**: кинематографичная заставка-интро + сложные спецэффекты на входе в направления (свои, в духе референсов), раскатать на все направления. Лучше — с вернувшимся предпросмотром, чтобы проверять глазами.
