# Лог сессии 2026-06-12 — публикация на euro.s.bg + план SEO (окно d3fff91c)

Запись хода работ (продолжение `2026-06-11_доработки.md`). Все версии кода — в git-истории
репозитория `GAGAUZ113/eurosfera-site`.

## 1. Второй телефон + проверка переводов (начало дня)
- В IT-страницу добавлен второй контакт **+359 89 209 84 58** (в CTA и подвал, под «дымкой» `.phone-mask`).
- Перепроверены переводы (EN+DE через Google Translate): всё переводится, бренды/телефоны не ломаются.
  Исправлен баг: «Бетон-лук/Дерево-лук» → чистые слова (Бетон/Камень/Древесина/Мрамор/Кортен),
  т.к. «лук» переводился как onion/bow. cache-bust v=0611f.

## 2. ПУБЛИКАЦИЯ САЙТА на главный домен https://euro.s.bg (SuperHosting.BG)
Анатолий решил: лить на ГЛАВНУЮ euro.s.bg вместо старого WordPress, но СНАЧАЛА бэкап; заливку — Claude сам.

Что сделано:
- В файлах репозитория адреса canonical/og/sitemap/robots переписаны с демо на `https://euro.s.bg`,
  пересобран `eurosfera-deploy.zip`.
- Анатолий скачал полный бэкап (cPanel → Создать резервную копию → Домашняя папка).
- Поставлен `lftp` (brew). Создан FTP-аккаунт **`deploy3@euro.s.bg`** с каталогом `public_html`
  (грабли SuperHosting: по умолчанию каталог = `<домен>/имя`, надо вручную вписать `public_html`).
  Пароль — в Связке ключей Мака: `security find-generic-password -s claude-ftp-eurosbgn -a deploy3 -w`.
- Залито по FTP: новый сайт замирорен в `public_html`; старый WordPress перенесён (не удалён!) в
  `public_html/OLD_WP/`; добавлен мини-`.htaccess` (DirectoryIndex index.html + ErrorDocument 404).
- Проверено: `https://euro.s.bg` и все 8 страниц + 404 + sitemap + robots → HTTP 200; следов WordPress 0;
  телефон в HTML скрыт (0 цифр); картинки грузятся. **Сайт ЖИВОЙ.**
- Почта euro.s.bg (sale@/it@/buh@) НЕ тронута — привязана к домену отдельно от файлов.
- ⚠️ К удалению: лишние FTP-аккаунты `deploy` и `deploy2` (пустые; у `deploy` пароль засветился в логах lftp).
  Память: `project_eurosfera_deploy_live.md`. Инструкция обновления: `DEPLOY.md`.

**Как обновлять сайт в будущем:** правки → `bash make-deploy.command` → распаковать zip в `/tmp/euro_site` →
`lftp -u deploy3@euro.s.bg,$PW ftp.euro.s.bg` → `mirror -R /tmp/euro_site .` (ssl-allow true; verify-cert no; passive).

## 3. Продвижение / SEO (обсуждение)
- Встроенные cPanel-метрики (AWStats/Посетители) = статистика посещений (зеркало), НЕ продвижение.
- Реальное продвижение:
  1. ⭐ **Google Search Console** — добавить `https://euro.s.bg`, подтвердить (HTML-файл заливает Claude по FTP),
     отправить `sitemap.xml`, «Запросить индексирование». Сроки: по названию/точным фразам — дни–2 недели;
     по конкурентным словам — недели–месяцы.
  2. **Google Analytics 4** — в сайте уже есть слот `EURO_GA4_ID` (вставить ID — Claude подключит).
  3. Языковые страницы под ЕС, ссылки, Google Business Profile, Google Ads.
- **Отчёты на почту:** в проекте уже есть модуль `analytics/` (пиксель `px.php` + крон `daily_report.php`,
  шлёт ежедневное письмо: страницы/посетители/источники). PHP теперь работает (SuperHosting) → активировать:
  вписать email в `daily_report.php`, залить, создать 1 крон-задачу в cPanel.

## Что Claude НЕ может сам
- Войти в Google-аккаунт (Search Console / Analytics) — только Анатолий (вход в чужой Google автоматом нельзя).
  Claude делает техчасть: файл-подтверждение, код GA4, почтовые отчёты. Google-клики — за Анатолием (с проводкой).
- Сгенерировать видео V-Class — нужен ИИ-видеосервис (Kling/Runway/Sora/Veo), генерит Анатолий; Claude вставит.
